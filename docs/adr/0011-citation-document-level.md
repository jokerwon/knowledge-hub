# ADR-0011：引用粒度为文档级

- 状态：已接受
- 日期：2026-08-28

## 背景

检索命中的是切片，引用呈现可选切片级（可展开原文片段）或文档级（仅标注来源文档）。

## 决策

- 引用以**文档级**呈现：答案附带来源文档列表（命中切片按文档去重）。
- 不提供切片原文的展开查看；切片内容仅作为 LLM 上下文在服务端使用。

## 后果

- web 引用 UI 与 API 契约最简；代价是用户无法直接核对答案依据的具体段落。
- 切片到文档的去重映射在 api 内完成，契约中不出现切片实体。
- 未来升级为切片级引用时，契约以新增字段方式演进。
